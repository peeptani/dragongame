import axios from 'axios'

const state = {
    shop: {}
}

const mutations = {}

const actions = {}


const getters = {
    shop: state => state.shop
}

export default {
    state,
    mutations,
    actions,
    getters
}